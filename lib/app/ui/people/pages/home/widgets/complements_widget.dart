import 'package:flutter/material.dart';

class ComplementsWidget extends StatelessWidget {
  final String complements;

  const ComplementsWidget({Key key, this.complements}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
